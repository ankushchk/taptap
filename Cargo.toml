[package]
name = "taptap"
version = "0.1.0"
edition = "2024"

[dependencies]
rdev = "0.5.3"
rodio = "0.17"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1"
dirs = "5"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
